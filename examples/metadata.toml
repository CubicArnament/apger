# Example metadata.toml — output format produced by apgbuild
# This is generated automatically; you do not write this by hand.

[package]
name = "cool-tool"
version = "1.0.0"
type = "binary"
architecture = "x86_64"
description = "Cool tool description"
maintainer = "AnmiTaliDev <anmitali198@gmail.com>"
license = "GPL-3.0"
homepage = "https://example.com"
tags = ["tool", "utility"]
dependencies = ["glibc", "zlib"]
conflicts = []
provides = []
replaces = []
conf = ["/etc/cool-tool.conf"]
